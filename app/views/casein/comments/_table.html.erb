<!-- Scaffolding generated by Casein v5.2.0.0 -->

<table class="table table-striped table-hover table-condensed">
  <thead>
    <tr>
      <th><%= casein_sort_link "Commenter", :commenter %></th>
      <th><%= casein_sort_link "Body", :body %></th>
      <th><%= casein_sort_link "Associated Story", :story_id %></th>
      <th>&nbsp;</th>
    </tr>
  </thead>

  <% comments.each do |comment| %>
    <tr>
      <td><%= casein_table_cell_link comment.commenter, casein_comment_path(comment) %></td>
      <td><%= casein_table_cell_link comment.body, casein_comment_path(comment) %></td>
      <td><%= casein_table_cell_link Story.find(comment.story_id).name, casein_comment_path(comment) %></td>
      <td class="delete">
        <%= link_to(casein_show_row_icon("trash"), casein_comment_path(comment), method: :delete, data: { confirm: "Are you sure you want to delete this comment?" }) %>
      </td>
    </tr>
  <% end %>
</table>
